{
  "enable_qq_to_mc_forward": {
    "description": "启用QQ群消息转发到MC",
    "type": "bool",
    "hint": "开启后，绑定了MC服务器的QQ群中的所有消息都会自动转发到游戏内（无需唤醒词）。修改后使用 /mcreload 命令重新加载配置即可生效，无需重启",
    "default": true
  },
  "qq_forward_message_color": {
    "description": "QQ消息在MC中的颜色",
    "type": "string",
    "hint": "QQ消息转发到MC时的文本颜色（十六进制颜色代码，例如 #00BFFF 是天蓝色）。修改后使用 /mcreload 命令重新加载配置即可生效，无需重启",
    "default": "#00BFFF"
  },
  "enable_join_quit_messages": {
    "description": "启用玩家进入/退出消息转发到QQ",
    "type": "bool",
    "hint": "开启后，玩家进入/退出游戏的消息会转发到绑定的QQ群。修改后使用 /mcreload 命令重新加载配置即可生效，无需重启",
    "default": true
  },
  "enable_mc_chat_to_qq_forward": {
    "description": "启用MC聊天自动转发到QQ",
    "type": "bool",
    "hint": "开启后，MC普通聊天会自动转发到QQ。为避免刷屏，默认不转发以唤醒词开头的消息（如 #qq/#help 等）。修改后使用 /mcreload 立即生效",
    "default": false
  },
  "mc_to_qq_forward_all_bound_groups": {
    "description": "MC聊天转发到所有已绑定群",
    "type": "bool",
    "hint": "开启后，MC聊天会发到 group_bindings.json 中所有服务器绑定过的QQ群（全局合并），无需每个服务器单独绑定。关闭则仅发到当前服务器绑定的群。修改后使用 /mcreload 立即生效",
    "default": false
  }
}
